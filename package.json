{
  "name": "cognal",
  "version": "0.1.0",
  "description": "Telegram bridge for Claude Code and Codex with single-active-agent runtime",
  "type": "module",
  "bin": {
    "cognal": "dist/cli.js",
    "cognald": "dist/daemon.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x dist/cli.js dist/daemon.js",
    "dev:cli": "tsx src/cli.ts",
    "dev:daemon": "tsx src/daemon.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@iarna/toml": "^2.2.5",
    "commander": "^14.0.2",
    "mime-types": "^3.0.1",
    "openai": "^6.7.0",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "@types/mime-types": "^3.0.1",
    "@types/node": "^24.5.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
